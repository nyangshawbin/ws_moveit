controller_list:
  - name: robot/arm_position_controller #make sure combination of <name> and <action_ns> (i.e. "<name>/<action>") is the full topic name shown with rostopic_list
    action_ns: follow_joint_trajectory
    type: FollowJointTrajectory
    default: true
    joints:
      - joint1
      - joint2
      - joint3  
      - joint4
      - joint5
      - joint6
